# Graph DFS

Depth-first traversal explores each branch fully before backtracking.

- **Time Complexity:** $O(N + M)$
- **Space Complexity:** $O(N)$

Use for connected components, traversal order, and cycle-related logic building blocks.

[View Full C++ Implementation](../../6_graphs/1_depth_first_search.cpp)
